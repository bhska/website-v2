export type { RoutesConfig } from './config';
export { routes, baseURL } from '@/resources/config';
export { person, contact, newsletter, home, about, blog, work, gallery } from '@/resources/content';
